storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=plyet
storeFile=android-release-key.keystore

# Legacy alternative: a single `password` field is still accepted as a
# fallback for both storePassword and keyPassword, e.g.:
# password=CHANGE_ME
